Blackridge production sandbox boundary — manual experiment record — 2026-08-27

Objective
---------
Reuse SWE-ReX and Docker to prepare an exact repository with network access, then run
the behavior-bearing workload without container networking or inherited host secrets.
Do not integrate the boundary into the production composer until paired real controls pass.

Calibration 1: internal network from container start — rejected
---------------------------------------------------------------
An exact SWE-ReX 1.4.0 deployment was started on a newly created Docker --internal
network. The deployment did not become ready because the same network restriction also
blocked the SWE-ReX host-to-container control channel. No workload result was produced,
so this attempt was not counted as a product PASS or FAIL.

After interruption, read-only inspection found exactly:
  network f29936fa4028 / blackridge-calibration-fcc8ba4b8e39
  container 129265bc4782 attached only to that network
Both exact objects were removed. Follow-up inspection reported:
  CONTAINER_REMAINING=False
  NETWORK_REMAINING=False

Calibration 2: prepare, detach, docker exec — boundary passed; cleanup design failed
-------------------------------------------------------------------------------
SWE-ReX started normally on bridge and returned version 1.4.0. The bridge network was
then detached and Docker inspection returned an empty network map. Shell-free docker
exec remained available and produced:
  workload control: exit 0, docker-exec-control
  direct 1.1.1.1:443: OSError [Errno 101] Network is unreachable
  pypi.org:443: gaierror [Errno -3] Temporary failure in name resolution
  sensitive environment names: []

SWE-ReX deployment.stop then returned ServerDisconnectedError because its server was
intentionally unreachable, and container 09c609155d3a remained. The exact container was
removed manually and follow-up inspection reported CONTAINER_REMAINING=False. This
attempt validated the split control plane but rejected SWE-ReX stop as the isolated
cleanup mechanism. The implementation now uses an exact recorded docker rm --force for
networkless executions and verifies that no container remains.

Frozen paired inputs
--------------------
Image ID:
  sha256:b1e8e07cc6cc02ad2319b76c893492e952920526e4638bcb102acf1c70ed6ad5
Repository:
  https://github.com/pypa/sampleproject
Commit:
  621e4974ca25ce531773def586ba3ed8e736b3fc
Preparation argv:
  python -m pip install --disable-pip-version-check .
Workload argv lists:
  baseline-unittest
  behavior-artifact
  network-and-secret-boundary
  boundary-sentinel

The retained request objects were compared after both runs. Image, commit, preparation
argv, and all four workload argv lists are identical. Only the declared execution
profile/network and descriptive metadata differ.

Positive production-boundary run
--------------------------------
Profile/network:
  production / none
Preparation:
  SWE-ReX fetched only the pinned commit and installed the exact checkout; exit 0.
Boundary evidence:
  networks before: bridge
  disconnect command exit: 0
  networks after: {}
  host environment forwarded by adapter: []
  workload executor: docker-exec-shell-free
Workload:
  repository unittest: exit 0, one test passed
  behavior artifact: {"actual": 42, "expected": 42, "input": 41}
  hostile boundary: {"direct_egress": false, "dns_egress": false,
                     "sensitive_environment_names": []}
  boundary sentinel: exit 0, production-boundary-complete
Host and cleanup:
  before/after host digest matched
  exact docker rm --force exit 0
  container remaining: false
  warnings: 0

Deliberately unisolated paired control
--------------------------------------
Profile/network:
  calibration / inherit
Baseline repository unittest and behavior artifact matched the positive run. The exact
hostile command then printed:
  {"direct_egress": true, "dns_egress": true,
   "sensitive_environment_names": []}
and exited 1 on the expected assertion. boundary-sentinel was explicitly retained as
not run. The host digest matched, SWE-ReX cleanup completed, and no container remained.

Fail-closed policy control
--------------------------
The positive production request was modified in memory only to request inherited
networking. Pydantic rejected it with the named cause:
  production sandbox experiments require execution_network=none
The set of matching Docker containers was identical before and after validation.

Regression and remote verification
----------------------------------
Local checks:
  ruff: pass
  generated third-party notices: match
  declared-copy provenance issues: 0
  pytest: 57 passed
  evidence JSON parsing: pass
  retained checksum comparison: pass
  local Markdown links: 0 missing
Implementation commit:
  48739d66679ec152ca9443c24ff657112be09cd0
GitHub Ubuntu CI (success):
  https://github.com/krapcys1-maker/blackridge-systems/actions/runs/33015018237
The GitHub job independently installed the project, linted it, verified notices and
copy provenance, and ran the regression suite. The Docker A/B remains the retained
manual evidence above; ordinary CI does not claim to replace that experiment.

Verdict and boundary
--------------------
PASS for the two-phase primitive on this frozen repository and image. The paired control
shows that the hostile check distinguishes isolated from inherited networking while
baseline behavior stays equal. This is not yet a production composer backend or a
hardened multi-tenant claim: preparation still has network access, the ephemeral
container filesystem is writable, the process runs as root inside the container
namespace, and Docker remains a trusted host boundary.

Generated-system sandbox experiment
-----------------------------------
The preserved `composer-positive-system` was selected before a general backend was
written. A one-off Docker experiment copied only `research_source.py` and
`report_sink.py` into the exact image without a host mount. Both in-container hashes
matched their locks, bridge was detached, each component consumed/emitted JSON through
stdin/stdout, the trusted host applied the locked adapter and all four schemas, and the
final report exactly matched the prior host calibration. Direct and DNS egress were
false; exact cleanup exited zero.

That first experiment exposed an existing host-runner evidence defect. python-json-patch
1.33 mutated the empty object held by the adapter's `add` operation when the later copy
wrote `/document/name`. The retained 2026-08-26 composer probe therefore showed a name
inside `operations[0].value`, while the locked generated adapter file still contained
`value: {}`. The runner now records one deep copy and applies a separate deep copy.
A real host rerun preserved `value: {}`, completed all steps, and returned the unchanged
final report.

Final `compose-run-sandbox` positive observation:
  provider: blackridge-generated-sandbox-runtime/1
  resolved image: sha256:b1e8e07cc6cc02ad2319b76c893492e952920526e4638bcb102acf1c70ed6ad5
  copied component hashes: 2/2 match
  networks before/after: bridge / {}
  preflight: direct=false, dns=false, sensitive_environment_names=[]
  source and sink executor: docker-exec-shell-free
  source, adapter, and sink output contracts: valid
  adapter retained add value: {}
  final artifact: exact match with host calibration
  cleanup exit: 0; container remaining: false; warnings: 0

Paired generated broken-output control:
  source and adapter completed
  broken sink process exit: 0
  output contract valid: false
  exact retained error paths: /report and /trace
  all_steps_completed: false
  preflight still passed
  cleanup exit: 0; container remaining: false

The backend remains calibration-only. Regression controls rewrite a temporary generated
runtime and its lock, then prove that `mode: production` and a nonempty environment
allowlist containing `DEEPSEEK_API_KEY` are rejected before Docker runtime types are
requested. A first CLI-help text assertion missed the Rich line wrap; direct help output
showed the command and the corrected whitespace-aware assertion passed. That harness
mistake was not counted as a backend failure.

Final artifact portability experiment
-------------------------------------
An initial attempt to invoke pytest through the unrelated global Python could not import
Blackridge. The repository venv then collected and passed the targeted composition suite;
the failed global collection was treated as an environment-selection error, not a product
verdict.

The first newly built wheel exposed a real portability defect that the editable checkout
did not expose. It was installed with the sandbox extra in a fresh venv and passed
`pip check`, but the generated bundle still declared the Python path captured from the
repository venv. The wheel venv had a different `sys.executable`; the backend rejected
that old absolute interpreter path and the real CLI run exited 2 before a component ran.
Cleanup still removed the prepared container.

The correction maps only argv position zero whose basename is `python`, `python.exe`,
`python3`, or `python3.exe` to the trusted container interpreter. The locked component
path is mapped separately, and any other absolute argv path still fails closed. A paired
regression maps a prior-venv Python path and rejects an extra unlocked absolute path.

A second clean wheel was built and installed in another new venv:
  wheel SHA-256: ff58425b672e3e6288f0e90074d7b418905821dc6270589a07af0063e361787a
  installed module: fresh venv site-packages, not the repository source tree
  pip check: no broken requirements
  real sandboxed generated-system CLI exit: 0
  effective component argv: python /workspace/components/component-1.py and component-2.py
  preflight: direct=false, dns=false, sensitive_environment_names=[]
  cleanup exit: 0; container remaining: false

The canonical positive and green-exit-invalid runs were then repeated once more against
the final source. Their exits were 0 and 1 respectively, both retained exact cleanup, and
their named manual reviews were regenerated against the new probe hashes. The inspection
harness initially queried `output_contract_errors` and a nonexistent top-level host
artifact path; inspecting the raw shape corrected those selectors to
`output_validation_errors` and `observations.runtime.observations.final_artifact`.
The corrected comparisons passed and the selector mistakes changed no product result.

Final local regression after the portability correction:
  ruff: pass
  generated third-party notices: match
  declared-copy provenance issues: 0
  pytest: 60 passed
